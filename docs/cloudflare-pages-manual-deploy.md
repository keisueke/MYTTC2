# Cloudflare Pages 手動デプロイガイド

## 概要

Wrangler CLIを使用して、Cloudflare Pagesに手動でデプロイする方法です。GitHub連携の自動デプロイが動作しない場合や、緊急でデプロイが必要な場合に使用します。

## 前提条件

1. Wrangler CLIがインストールされている
   ```bash
   npm install -g wrangler
   ```

2. Cloudflareにログインしている
   ```bash
   wrangler login
   ```

3. Cloudflare Pagesプロジェクトが作成されている
   - Dashboardで作成済み、または
   - `wrangler pages project create mytcc2`で作成

## 手動デプロイの手順

### 方法1: npmスクリプトを使用（推奨）

```bash
# ビルドとデプロイを一度に実行
npm run deploy:cloudflare:project
```

このコマンドは以下を実行します：
1. `npm run build` - アプリをビルド
2. `wrangler pages deploy dist --project-name=mytcc2` - Cloudflare Pagesにデプロイ

### 方法2: 手動でコマンドを実行

```bash
# 1. ビルド
npm run build

# 2. デプロイ（プロジェクト名を指定）
wrangler pages deploy dist --project-name=mytcc2

# または、プロジェクト名を省略（自動検出）
wrangler pages deploy dist
```

### 方法3: プロジェクト名を確認してからデプロイ

```bash
# プロジェクト一覧を確認
wrangler pages project list

# プロジェクト名を確認後、デプロイ
wrangler pages deploy dist --project-name=<実際のプロジェクト名>
```

## プロジェクト名の確認方法

### Cloudflare Dashboardで確認

1. [Cloudflare Dashboard](https://dash.cloudflare.com)にアクセス
2. 「Workers & Pages」→「Pages」を選択
3. プロジェクト名を確認（例: `mytcc2`）

### Wrangler CLIで確認

```bash
wrangler pages project list
```

## デプロイの確認

デプロイが成功すると、以下のような出力が表示されます：

```
✨ Compiled Worker successfully
🌍 Uploading...
✨ Uploaded Worker successfully
Published mytcc2 (123.456ms)
  https://mytcc2.pages.dev
```

## トラブルシューティング

### エラー: "Project not found"

**原因**: プロジェクト名が間違っている、またはプロジェクトが存在しない

**解決方法**:
1. `wrangler pages project list`でプロジェクト名を確認
2. 正しいプロジェクト名を指定
3. プロジェクトが存在しない場合は、Dashboardで作成

### エラー: "Must specify a production branch"

**原因**: プロジェクトがGitHub連携で作成されている場合、手動デプロイが制限される可能性

**解決方法**:
1. Dashboardでプロジェクト設定を確認
2. 手動デプロイが許可されているか確認
3. 必要に応じて、新しいプロジェクトを作成（手動デプロイ専用）

### エラー: "No files to deploy"

**原因**: `dist`ディレクトリが存在しない、または空

**解決方法**:
1. `npm run build`を実行してビルド
2. `ls -la dist/`でファイルが生成されているか確認
3. `dist/index.html`が存在するか確認

## 自動デプロイとの使い分け

### 自動デプロイ（GitHub連携）

- **用途**: 通常の開発フロー
- **方法**: GitHubにプッシュするだけで自動デプロイ
- **メリット**: 手動操作が不要

### 手動デプロイ（Wrangler CLI）

- **用途**: 緊急デプロイ、自動デプロイが動作しない場合
- **方法**: `npm run deploy:cloudflare:project`
- **メリット**: 即座にデプロイ可能

## 参考リンク

- [Wrangler Pages Documentation](https://developers.cloudflare.com/workers/wrangler/commands/#pages-deploy)
- [Cloudflare Pages Manual Deployment](https://developers.cloudflare.com/pages/platform/deploy-via-wrangler/)

